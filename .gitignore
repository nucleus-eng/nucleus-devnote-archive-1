.DS_Store
node_modules/
_build/
# shared curvenote plugin deps, committed so CI (which never runs npm
# install for a devnote's own package.json) can actually load them
!/plugins/seqviz/node_modules/
.ipynb_checkpoints/
__pycache__/
*.pyc
.git.disable/
*.mp4
archive/

# Artifacts of the glossary check, written by the workflow and by a local run
# of the same commands.
vale.json
changed.txt
comment.md
